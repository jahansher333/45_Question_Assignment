//Question 3: Name Cases: Store a person’s name in a variable, 
//and then print that person’s name in lowercase, uppercase, and titlecase
var person = "Jahansher";
console.log(person.toLowerCase()); //The show is small letter
console.log(person.toUpperCase()); //The show is big letter
console.log(person.charAt(0).toUpperCase());
console.log(person.slice(1).toLowerCase()); //show the name with first letter big letter
